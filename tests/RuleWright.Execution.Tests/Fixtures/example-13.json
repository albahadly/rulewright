{
  "ruleSet": {
    "name": "Free-shipping messaging (if / else)",
    "description": "A rule fires actions only when its condition matches, so an \u0027else\u0027 is a second rule with the negated condition. Here one branch appends a positive message and the other appends a different one; the two conditions are mutually exclusive, so exactly one appends to Messages. Note the condition is written twice (once plain, once inside NOT) and must be kept in sync \u2014 see 14-if-else-decision-table.json for the DRY equivalent.",
    "rules": [
      {
        "id": "qualifies",
        "description": "Condition met: append the positive message.",
        "condition": {
          "field": "Order.Total",
          "operator": "GreaterThanOrEqual",
          "value": 100
        },
        "actions": [
          {
            "type": "appendToOutput",
            "target": "Messages",
            "value": "You qualify for free shipping!"
          }
        ]
      },
      {
        "id": "does-not-qualify",
        "description": "Same condition negated with a NOT group: append a different message.",
        "condition": {
          "type": "group",
          "operator": "NOT",
          "rules": [
            {
              "field": "Order.Total",
              "operator": "GreaterThanOrEqual",
              "value": 100
            }
          ]
        },
        "actions": [
          {
            "type": "appendToOutput",
            "target": "Messages",
            "value": "Add more to your cart to unlock free shipping."
          }
        ]
      }
    ]
  },
  "fact": {
    "Order": {
      "Total": 150
    }
  },
  "expected": {
    "firedRules": [
      "qualifies"
    ],
    "outputs": {
      "Messages": [
        "You qualify for free shipping!"
      ]
    }
  }
}